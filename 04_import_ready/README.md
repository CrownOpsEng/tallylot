# Final Accepted Import Files

Copy only approved, overlap-screened import files here.

These files are still derivatives. The raw evidence stays under `01_raw_exports/external/`.

Do not copy a file here until `06_scripts/overlap_check.py` returns `status: "pass"` for the final candidate.
