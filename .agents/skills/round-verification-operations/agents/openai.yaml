interface:
  display_name: "Round Verification"
  short_description: "Run tallylot's oracle screen, stage, and round verification path."
  default_prompt: "Use $round-verification-operations to run tallylot's documented oracle screen, stage, and round verification workflow."

policy:
  allow_implicit_invocation: true
