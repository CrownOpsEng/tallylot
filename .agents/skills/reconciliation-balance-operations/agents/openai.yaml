interface:
  display_name: "Reconciliation Balances"
  short_description: "Run balance inspection, checks, and summaries."
  default_prompt: "Use $reconciliation-balance-operations to inspect balances, run checks offline by default, and summarize reconciliation dates."

policy:
  allow_implicit_invocation: true
