interface:
  display_name: "Reconciliation Balances"
  short_description: "Run balance coverage, checks, and summaries."
  default_prompt: "Use $reconciliation-balance-operations to inspect balance coverage, run checks, and summarize reconciliation dates."

policy:
  allow_implicit_invocation: true
