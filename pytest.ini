[pytest]
addopts = -ra
testpaths = tests
markers =
    e2e: CLI-level script execution.
    repo_data: requires checked-in repo exports, checkpoints, or live workspace evidence.
    slow: materially slower coverage that is not needed for every fast feedback run.
