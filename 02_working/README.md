# Working Area

This tree holds derived files used to prepare imports and verify CoinTracking changes.

Subfolders:

- `normalized/` for cleaned but not yet approved source files
- `import_batches/` for reviewed import candidates
- `verification/` for fresh CoinTracking exports captured after each repair or import round

Nothing here is a raw source of truth.
